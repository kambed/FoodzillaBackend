spring:
  graphql:
    graphiql:
      enabled: true
  datasource:
    username: sa
    password: password
    url: jdbc:h2:file:~/rest-fodzilla
  jpa:
    hibernate:
      ddl-auto: create-drop
  h2:
    console:
      enabled: true
  data:
    redis:
      port: ${REDIS_TEST_PORT}
      run-sync-task: false