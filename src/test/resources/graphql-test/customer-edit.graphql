mutation edit(
    $firstname: String!,
    $lastname: String!
    $username: String!
    $password: String!) {
    editCustomer(
        customer: {
            firstname: $firstname
            lastname: $lastname
            username: $username
            password: $password
        }
    ) {
        firstname
        lastname
        username
    }
}