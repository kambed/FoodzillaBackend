mutation login($username: String!,
    $password: String!) {
    login(
        username: $username
        password: $password
    ) {
        token
        customer {
            firstname
            lastname
            username
        }
    }
}