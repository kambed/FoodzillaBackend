mutation($id: Int) {
    deleteSavedSearch(id: $id) {
        id
        phrase
        filters {
            attribute
            equals
        }
        sort {
            attribute
            direction
        }
    }
}